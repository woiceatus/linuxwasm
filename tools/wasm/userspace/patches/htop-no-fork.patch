--- a/OpenFilesScreen.c
+++ b/OpenFilesScreen.c
@@ -106,6 +106,13 @@
       return pdata;
    }
 
+#if 1 /* wasm32-linux: no fork(); disable lsof helper */
+   close(fdpair[1]);
+   close(fdpair[0]);
+   pdata->error = 1;
+   return pdata;
+#else
    pid_t child = fork();
    if (child == -1) {
       close(fdpair[1]);
@@ -160,6 +167,7 @@
 
    return pdata;
 }
+#endif
 
 void OpenFilesScreen_fixItem(Panel* this, ListItem* item) {
    assert(Object_isA((Object*) item, (const ObjectClass*) &ListItem_class));
